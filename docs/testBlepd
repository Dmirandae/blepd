library(blepd)
data(distribution)
data(tree)


evalTree(tree, distribution)

swapBL(tree, distribution)
swapBL(tree, distribution,model="uniform")
swapBL(tree, distribution,model="allswap")


print("Simulated values, uniform dist 0-1")

newTree <- tree

num <- length(newTree$edge.length)

 valoresUnif <- runif(num,0,1)
        
        newTree$edge.length <- valoresUnif 

swapBL(newTree, distribution,model="uniform")


print("Simulated values, normal dist 0-1")


newTree <- tree

num <- length(newTree$edge.length)

 valoresUnif <- rnorm(num)
        
        newTree$edge.length <- valoresUnif 



print("testing PE")
swapBL(newTree, distribution,model="uniform", index ="PE")

lengthTerminals(newTree)

for ( numTree in 1:3){

tmpTree <- reorder.phylo(RhinoclemmysData$trees[[numTree]], order = "post")

plot.phylo(tmpTree)

print(lengthTerminals(RhinoclemmysData$trees[[numTree]]))

}


#evalTree(tree, as.data.frame(distribution))

#swapBL(newTree, distribution,model="uniform", nTimes=1000)

#swapBL(newTree, distribution,model="allswap", nTimes=1000)

#swapBL(newTree, distribution,model="Uniform") ## to check the error





